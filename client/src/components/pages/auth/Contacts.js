import React from "react";
import ContactsList from "../../ContactsList.js";

const Contacts = () => {
    return (
        <div>
            <h1>Contacts</h1>
            <ContactsList />
        </div>
    );
};

export default Contacts;
